test F X is
    call (call F on X) on X.

identity X is X.

secondTest X is
    call (test _ X) on (identity _).

add X Y is X + Y.

const X Y is X.

display test (const _ _) 24.
display as text test (add _ _) 1.
display as text map (add 4 _) over (range 1 to 20).

