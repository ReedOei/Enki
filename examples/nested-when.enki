do not use base.

built in 'TypedRule (Comp [S "writeln", V "Str"]) EnkiString (TypedConstraints [])'

- X is 0 - X.

display X does writeln X.

f X is
    when X > 5 then
        when X > 10 then "more than 10";
        when X > 7 then "more than 7";
        default "at least 5";
    when X < 0 then "negative";
    default "blah".

display f (-1).
display f 3.
display f 6.
display f 10.
display f 20.

