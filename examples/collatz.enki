do not use base.

collatz X is N where
    when X = 2*K + 1 then N = 3*X + 1;
    default X = 2*N.

collatz sequence on X takes N steps if
    when X = 1 then N = 0;
    default
        collatz X is K,
        collatz sequence on K takes (N - 1) steps.

